<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit.good.dao.LoginDao">
	<select id="loginUser" resultType="com.bit.good.dto.JoinDto">
		SELECT * FROM USERLIST WHERE EMAIL = #{param1} AND PW = #{param2}
	</select>
	
</mapper>