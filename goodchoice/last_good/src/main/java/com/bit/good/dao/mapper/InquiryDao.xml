<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit.good.dao.InquiryDao">
	<select id="listDao" resultType="com.bit.good.dto.InquiryDto">
		SELECT * FROM INQUIRY ORDER BY NUM DESC
	</select>
	<insert id="inquiryWriteDao">
		INSERT INTO INQUIRY (NUM, EMAIL, CATEGORY, TYPE, PHONE, CONTENTS) VALUES (INQUIRY_SEQ.NEXTVAL, #{param1}, #{param2}, #{param3}, #{param4}, #{param5})
	</insert>
</mapper>